---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import BrandMark from '../components/BrandMark.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import MeteorShower from '../components/MeteorShower.astro';
import MobileDrawer from '../components/MobileDrawer.astro';
import ParthenonColumns from '../components/ParthenonColumns.astro';
import PostCard from '../components/PostCard.astro';
import TerminalQuote from '../components/TerminalQuote.astro';
import { BILIBILI_URL, CREATINF_URL, GITHUB_URL, SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const BASE = import.meta.env.BASE_URL;
// Keep home preview aligned with list pagination size for consistent editorial cadence.
const posts = (await getCollection('blog'))
	.filter((post) => !import.meta.env.PROD || post.data.draft !== true)
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 6);

const HOME_WALLPAPER = '/img/IMG_7456.GIF';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`${SITE_TITLE} - Research, Engineering & Technical Notes`} description="A personal blog about research, engineering workflows, and reproducible long-form writing.记录生活。" />
	</head>
	<body>
		<ParthenonColumns />
		<MeteorShower density={0.45} heightVh={46} />
		<Header />
		<MobileDrawer />
		<main class="page-shell page-fade-in py-10 md:py-14" transition:name="page-main" transition:animate="fade">
			<div class="space-y-6">
				<section class="hero-enter overflow-hidden rounded-2xl border border-zinc-200 bg-white dark:border-zinc-700 dark:bg-zinc-900">
					<div class="relative h-[44vh] min-h-[300px] w-full">
						<img
							src={HOME_WALLPAPER}
							alt="Homepage wallpaper"
							class="hero-kenburns h-full w-full object-cover object-[77%_center] md:object-center"
						/>
						<div class="absolute inset-0 bg-zinc-900/10 dark:bg-zinc-950/25"></div>
						<div class="absolute inset-x-0 bottom-0 h-1/3 bg-gradient-to-t from-zinc-950/35 to-transparent"></div>
					</div>
				</section>

				<section class="section-enter section-enter-delay-1 grid grid-cols-1 gap-6 items-start md:grid-cols-[minmax(0,1fr)_auto] md:items-center">
					<div class="hero-enter hero-enter-delay-2 max-w-[40rem]">
						<h1 class="brand-wordmark flex flex-wrap items-center gap-x-2.5 gap-y-1" transition:name="page-title" transition:animate="fade">
							<BrandMark class="h-[2.3rem] -ml-1 self-center md:h-[3.05rem]" />
							<span class="text-[2.55rem] leading-none text-zinc-900 dark:text-zinc-100 md:text-[3.4rem]">
								我<span class="brand-apostrophe">&rsquo;</span>的
							</span>
							<span class="text-[2.55rem] italic leading-none text-zinc-600 dark:text-zinc-300 md:text-[3.4rem]">
								Blog
							</span>
						</h1>
						<p class="mt-3 max-w-xl font-sans text-[15px] leading-7 text-zinc-500 dark:text-zinc-400">
							个人记录.
						</p>
						<div class="mt-3 flex flex-wrap items-center gap-2 text-[11px] font-semibold uppercase tracking-[0.16em] text-zinc-400 dark:text-zinc-500">
							<span class="apple-meta-pill">
								<span class="apple-meta-pill-label">生活</span>
							</span>
							<span class="apple-meta-pill">
								<span class="apple-meta-pill-label">记录</span>
							</span>
							<span class="apple-meta-pill">
								<span class="apple-meta-pill-label">管人</span>
							</span>
						</div>
						<div class="mt-3.5 flex flex-wrap gap-2.5">
							<span class="inline-flex items-center rounded-full border border-zinc-200/85 bg-white/78 px-3 py-1.5 text-[11px] font-medium tracking-[0.01em] text-zinc-600 shadow-[0_8px_20px_rgba(15,23,42,0.04)] backdrop-blur-sm dark:border-zinc-700/80 dark:bg-zinc-900/72 dark:text-zinc-300">
								UI: 中国
							</span>
							<span class="inline-flex items-center rounded-full border border-zinc-200/85 bg-white/78 px-3 py-1.5 text-[11px] font-medium tracking-[0.01em] text-zinc-600 shadow-[0_8px_20px_rgba(15,23,42,0.04)] backdrop-blur-sm dark:border-zinc-700/80 dark:bg-zinc-900/72 dark:text-zinc-300">
								Content: Chinese (long-form)
							</span>
						</div>
						<div class="mt-3.5 flex items-center gap-2">
							<a
								href={GITHUB_URL}
								target="_blank"
								rel="noreferrer"
								class="icon-button inline-flex h-10 w-10 items-center justify-center rounded-xl border border-zinc-200 text-zinc-600 transition-colors hover:border-zinc-400 hover:text-zinc-900 dark:border-zinc-700 dark:text-zinc-300 dark:hover:border-zinc-500 dark:hover:text-zinc-100"
								aria-label="GitHub"
							>
								<svg viewBox="0 0 24 24" class="h-5 w-5 fill-current" aria-hidden="true">
									<path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.523 2 12 2z" />
								</svg>
								<span class="sr-only">GitHub</span>
							</a>
							<a
								href={BILIBILI_URL}
								target="_blank"
								rel="noreferrer"
								class="icon-button inline-flex h-10 w-10 items-center justify-center rounded-xl border border-zinc-200 text-zinc-600 transition-colors hover:border-zinc-400 hover:text-zinc-900 dark:border-zinc-700 dark:text-zinc-300 dark:hover:border-zinc-500 dark:hover:text-zinc-100"
								aria-label="Bilibili"
							>
								<svg viewBox="0 0 24 24" class="h-5 w-5 fill-current" aria-hidden="true">
									<path d="M7.4 2.8a1 1 0 0 1 1.4 0L12 6l3.2-3.2a1 1 0 1 1 1.4 1.4L14.5 6.3h3.8A3.7 3.7 0 0 1 22 10v8.3A3.7 3.7 0 0 1 18.3 22H5.7A3.7 3.7 0 0 1 2 18.3V10a3.7 3.7 0 0 1 3.7-3.7h3.8L7.4 4.2a1 1 0 0 1 0-1.4ZM5.7 8.3A1.7 1.7 0 0 0 4 10v8.3c0 .9.8 1.7 1.7 1.7h12.6c.9 0 1.7-.8 1.7-1.7V10c0-.9-.8-1.7-1.7-1.7H5.7Zm1.8 3a1 1 0 0 1 1 1v3.4a1 1 0 1 1-2 0v-3.4a1 1 0 0 1 1-1Zm9 0a1 1 0 0 1 1 1v3.4a1 1 0 1 1-2 0v-3.4a1 1 0 0 1 1-1Z" />
								</svg>
								<span class="sr-only">Bilibili</span>
							</a>
							<a
								href={CREATINF_URL}
								target="_blank"
								rel="noreferrer"
								class="icon-button inline-flex h-10 w-10 items-center justify-center rounded-xl border border-zinc-200 text-zinc-600 transition-colors hover:border-zinc-400 hover:text-zinc-900 dark:border-zinc-700 dark:text-zinc-300 dark:hover:border-zinc-500 dark:hover:text-zinc-100"
								aria-label="CreatInf"
							>
								<span class="text-lg font-semibold leading-none">∞</span>
								<span class="sr-only">CreatInf</span>
							</a>
						</div>
					</div>

					<div class="hero-enter hero-enter-delay-2 w-full max-w-sm justify-self-end md:self-start">
						<TerminalQuote />
					</div>
				</section>

				<!-- Decorative pop-art ribbon spanning the full hero row width. -->
				<section class="hero-enter hero-enter-delay-2 -mt-1" aria-hidden="true">
					<div class="dots-band" data-dots-band>
						<canvas></canvas>
					</div>
				</section>
			</div>

			<section class="mt-10">
				<div class="mb-4 flex items-center justify-between" data-reveal>
					<h2 class="text-xl font-semibold tracking-tight">文章预览</h2>
					<a
						href={`${BASE}blog/`}
						class="apple-float-chip inline-flex items-center gap-1.5 rounded-xl border border-zinc-300/90 bg-zinc-50/96 px-3 py-1.5 text-sm font-medium text-zinc-700 no-underline dark:border-zinc-700/90 dark:bg-zinc-900/96 dark:text-zinc-300"
					>
						<span class="apple-float-label">
							<span>查看所有</span>
							<svg viewBox="0 0 20 20" class="h-4 w-4 opacity-75" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
								<path d="M4.75 10h10.5" stroke-linecap="round" />
								<path d="M10.75 5.5 15.25 10l-4.5 4.5" stroke-linecap="round" stroke-linejoin="round" />
							</svg>
						</span>
					</a>
				</div>
				<ul class="grid gap-4 md:grid-cols-2">
					{
						posts.map((post, idx) => (
							<li class="relative" data-reveal style={`--reveal-delay: ${(idx % 2) * 80}ms`}>
								<PostCard
									post={post}
									base={BASE}
									titleTag="h3"
									titleClass="mt-2 text-lg font-semibold tracking-tight text-zinc-900 dark:text-zinc-100"
									descriptionClass="mt-2 font-serif text-sm leading-7 text-zinc-600 dark:text-zinc-300"
									maxMobile={3}
									maxDesktop={3}
									reloadOnNavigate={true}
								/>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />

		<script is:inline>
			(() => {
				/*
				 * Pop-art dot ribbon: a drifting halftone wave on <canvas>.
				 * Gold dots with sparse blue accents and hollow rings; brightness and
				 * size follow two layered sine waves so the band reads as a slow,
				 * left-to-right current with twinkling crests.
				 * Pauses off-screen / in hidden tabs; reduced motion gets one static frame.
				 */
				const initDotsBand = () => {
					const wrap = document.querySelector('[data-dots-band]');
					if (!wrap || wrap.dataset.bound === 'true') return;
					wrap.dataset.bound = 'true';

					const canvas = wrap.querySelector('canvas');
					const ctx = canvas?.getContext('2d');
					if (!ctx) return;

					const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
					const SPACING = 17;
					const ROWS = 3;

					let width = 0;
					let height = 0;
					let dots = [];

					const build = () => {
						const dpr = Math.min(window.devicePixelRatio || 1, 2);
						const rect = wrap.getBoundingClientRect();
						width = rect.width;
						height = rect.height;
						canvas.width = Math.max(1, Math.round(width * dpr));
						canvas.height = Math.max(1, Math.round(height * dpr));
						ctx.setTransform(dpr, 0, 0, dpr, 0, 0);

						dots = [];
						const cols = Math.ceil(width / SPACING) + 1;
						for (let c = 0; c < cols; c++) {
							for (let r = 0; r < ROWS; r++) {
								dots.push({
									x: c * SPACING,
									row: r,
									phase: Math.random() * Math.PI * 2,
									twinkle: 0.6 + Math.random() * 0.9,
									accent: Math.random() < 0.055,
									ring: Math.random() < 0.075,
								});
							}
						}
					};

					let t = Math.random() * 100;
					let rafId = 0;
					let running = false;

					const draw = () => {
						ctx.clearRect(0, 0, width, height);
						const dark = document.documentElement.classList.contains('dark');
						const base = dark ? '226, 190, 110' : '170, 118, 32';
						const accent = dark ? '96, 165, 250' : '37, 99, 235';
						const rowGap = height / (ROWS + 1);

						for (const d of dots) {
							// Primary wave travels right; the slower secondary wave breaks uniformity.
							const wave = Math.sin(d.x * 0.018 - t * 1.55 + d.row * 0.85);
							const drift = Math.sin(d.x * 0.0065 + t * 0.6 + d.phase);
							const y = rowGap * (d.row + 1) + wave * 7 + drift * 3;
							const crest = (wave + 1) / 2;
							const pulse = (Math.sin(t * d.twinkle * 2 + d.phase) + 1) / 2;
							const alpha = 0.07 + crest * 0.3 + pulse * 0.22 * crest;
							const radius = 1.05 + crest * 1.25 + pulse * 0.65;
							const rgb = d.accent ? accent : base;

							if (d.ring) {
								ctx.strokeStyle = `rgba(${rgb}, ${Math.min(alpha * 1.15, 0.6)})`;
								ctx.lineWidth = 1;
								ctx.beginPath();
								ctx.arc(d.x, y, radius + 1.4, 0, Math.PI * 2);
								ctx.stroke();
							} else {
								ctx.fillStyle = `rgba(${rgb}, ${alpha})`;
								ctx.beginPath();
								ctx.arc(d.x, y, radius, 0, Math.PI * 2);
								ctx.fill();
							}
						}
					};

					const tick = () => {
						t += 0.016;
						draw();
						rafId = requestAnimationFrame(tick);
					};

					let destroyed = false;
					let inView = true;
					const start = () => {
						if (running || destroyed || reducedMotion) return;
						running = true;
						rafId = requestAnimationFrame(tick);
					};
					const stop = () => {
						running = false;
						if (rafId) cancelAnimationFrame(rafId);
						rafId = 0;
					};

					let resizeRaf = 0;
					let io = null;
					const onVisibility = () => {
						if (document.hidden || !inView) stop();
						else start();
					};
					const onResize = () => {
						if (resizeRaf) return;
						resizeRaf = requestAnimationFrame(() => {
							resizeRaf = 0;
							build();
							draw();
						});
					};

					// Full teardown so route swaps and tab switches can never revive a
					// loop that draws to a detached canvas.
					const destroy = () => {
						destroyed = true;
						stop();
						if (resizeRaf) cancelAnimationFrame(resizeRaf);
						resizeRaf = 0;
						if (io) io.disconnect();
						document.removeEventListener('visibilitychange', onVisibility);
						window.removeEventListener('resize', onResize);
						document.removeEventListener('astro:before-swap', destroy);
					};

					// Replace any instance left over from a previous route.
					if (typeof window.__dotsBandDestroy === 'function') window.__dotsBandDestroy();
					window.__dotsBandDestroy = destroy;

					build();
					draw();

					if (!reducedMotion) {
						io = new IntersectionObserver((entries) => {
							for (const entry of entries) {
								inView = entry.isIntersecting;
								if (inView && !document.hidden) start();
								else stop();
							}
						});
						io.observe(wrap);
						document.addEventListener('visibilitychange', onVisibility);
					}

					window.addEventListener('resize', onResize);
					document.addEventListener('astro:before-swap', destroy);
				};

				// Keep exactly one document-level page-load hook no matter how many times
				// this inline script re-executes across View Transition navigations.
				window.__dotsBandInit = initDotsBand;
				if (!window.__dotsBandPageLoadBound) {
					window.__dotsBandPageLoadBound = true;
					document.addEventListener('astro:page-load', () => {
						if (typeof window.__dotsBandInit === 'function') window.__dotsBandInit();
					});
				}
				initDotsBand();
			})();
		</script>
	</body>
</html>
